# typescript
```bash
bash /vagrant/install_dotfiles.sh
bash /vagrant/install_nodejs.sh
# Install typescript related tools globally. Alternatively, you can use a
# `package.json` to install them locally.
bash /vagrant/install_typescript.sh
```
